//55

int sum10(int a, int b, int c, int d, int e, int f, int g, int h, int i, int k) {
    return a + b + c + d + e + f + g + h + i + k; 
}

int main() {
    int ten = 10; 
    int eight = 8; 
    int result = sum10(1, 2, 3, 4, 5, 6, 7, eight, 9, ten);
    return result;
}
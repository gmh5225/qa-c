// 0

int main() {
    int x = 0;
    x = x + 1; // 1
    x = x + 2; // 3
    x = x + 3; // 6
    x = x + 4; // 10
    x = x - 1; // 9
    x = x - 2; // 7
    x = x - 3; // 4
    x = x - 4; // 0
    
    return x; 
}